OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-5.1

# Shared password for the deployed app (src/proxy.ts). Any username works.
# Leave unset locally to skip the prompt; required in production, where the app
# refuses all requests without it.
APP_PASSWORD=
